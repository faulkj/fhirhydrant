[
   {
      "name": "paginate",
      "description": "Fetch a single page of FHIR Bundle results using a pagination URL. The url must come from a FHIR Bundle's link array where relation is \"next\". Do not construct pagination URLs manually — only use links returned by the FHIR server.",
      "params": {
         "url": {
            "type": "string",
            "description": "Pagination URL from a FHIR Bundle link[rel=next].url value"
         },
         "fhirpath": {
            "type": "string",
            "optional": true,
            "description": "FHIRPath expression evaluated locally against the FHIR response — use for client-side projection when _elements or _summary are unavailable or insufficient"
         },
         "responseMode": {
            "type": "string",
            "optional": true,
            "description": "Response shape: compact or full"
         },
         "maxResults": {
            "type": "number",
            "optional": true,
            "description": "Target number of compact results — server stops fetching additional pages once this threshold is reached"
         },
         "prefetch": {
            "type": "string",
            "optional": true,
            "description": "Set to false to disable server-side page coalescing"
         }
      }
   },
   {
      "name": "capabilities",
      "description": "Returns a summary of the FHIR server's capabilities from /metadata (CapabilityStatement). Shows supported resource types, their interactions, search parameters, and any tools that were skipped. Call this before clinical queries to understand what the server supports. Set refresh=true to re-fetch /metadata from the server.",
      "params": {
         "refresh": {
            "type": "boolean",
            "optional": true,
            "description": "Re-fetch /metadata from the server before returning the summary"
         }
      }
   },
   {
      "name": "terminology_lookup",
      "description": "Look up a single medical code in a terminology system and return its display name, version, and properties. Use this when you already have a specific code (e.g. LOINC 2160-0 or SNOMED 73211009) and need to know what it means. system accepts \"loinc\" or \"snomed\".",
      "params": {
         "system": {
            "type": "string",
            "description": "Target terminology system: \"loinc\" or \"snomed\""
         },
         "code": {
            "type": "string",
            "description": "The code to look up (e.g. \"2160-0\" for LOINC, \"73211009\" for SNOMED)"
         }
      }
   },
   {
      "name": "code_search",
      "description": "Search a terminology system for codes matching a text filter. Use this when you need to find codes by name or description (e.g. \"glucose\" in LOINC or \"myocardial infarction\" in SNOMED). Returns matching code/display pairs. LOINC results support paging via offset — increment offset by count to retrieve subsequent pages. system accepts \"loinc\" or \"snomed\".",
      "params": {
         "system": {
            "type": "string",
            "description": "Target terminology system: \"loinc\" or \"snomed\""
         },
         "filter": {
            "type": "string",
            "description": "Text to search for (e.g. \"glucose\", \"diabetes mellitus\")"
         },
         "count": {
            "type": "number",
            "optional": true,
            "description": "Maximum results to return (default 10, max 50)"
         },
         "offset": {
            "type": "number",
            "optional": true,
            "description": "Starting position for paged results (default 0). Increment by count for subsequent pages."
         }
      }
   },
   {
      "name": "bundle",
      "description": "Submit a FHIR batch or transaction Bundle to the server. Requires FHIR_BUNDLE_CAPABILITIES to be configured. Write entries require additional opt-in via FHIR_BUNDLE_WRITES_ENABLED.",
      "params": {
         "body": {
            "type": "string",
            "description": "Complete FHIR Bundle JSON with resourceType, type (batch or transaction), and entry array"
         },
         "fhirpath": {
            "type": "string",
            "optional": true,
            "description": "FHIRPath expression evaluated locally against the response Bundle"
         },
         "responseMode": {
            "type": "string",
            "optional": true,
            "description": "Response shape: compact or full"
         }
      }
   },
   {
      "name": "system_history",
      "description": "Retrieve system-level change history from the FHIR server. Returns a Bundle of all resource versions across all types. Only available when the server advertises the system history interaction.",
      "params": {
         "_since": {
            "type": "string",
            "optional": true,
            "description": "Only include versions created at or after this date/dateTime"
         },
         "_at": {
            "type": "string",
            "optional": true,
            "description": "Only include versions current at this instant"
         },
         "_count": {
            "type": "number",
            "optional": true,
            "description": "Maximum number of history entries to return per page"
         },
         "fhirpath": {
            "type": "string",
            "optional": true,
            "description": "FHIRPath expression evaluated locally against the response Bundle"
         },
         "responseMode": {
            "type": "string",
            "optional": true,
            "description": "Response shape: compact or full"
         },
         "maxResults": {
            "type": "number",
            "optional": true,
            "description": "Target number of compact results — server stops fetching additional pages once this threshold is reached"
         },
         "prefetch": {
            "type": "string",
            "optional": true,
            "description": "Set to false to disable server-side page coalescing"
         }
      }
   }
]
